program memori;
uses crt;
var ct,cd,cf,ut,ud,uf,rt,rd,rf,et,ed,ef,tt,td,tf :integer;
begin
     clrscr;
      ct:=640; cd:=40; cf:=ct-cd;
      ut:=123; ud:=123; uf:=ut-ud;
      rt:=384; rd:=348; rf:=rt-rd;
      et:=7045; ed:=6021; ef:=et-ed;
      tt:=ct+ut+rt+et;
      td:=cd+ud+rd+ed;
      tf:=cf+uf+rf+ef;
      textcolor(lightblue);
      gotoxy(25,1);
      writeln('Tabel memori');
      textcolor(blue);
      writeln('зддддддддддддддддбддддддддддддбдддддддддддддбддддддддддддддд©');
      writeln('Ё                Ё            Ё             Ё               Ё');
      writeln('цддддддддддддддддеддддддддддддедддддддддддддеддддддддддддддд╢');
      writeln('Ё                Ё            Ё             Ё               Ё');
      writeln('цддддддддддддддддеддддддддддддедддддддддддддеддддддддддддддд╢');
      writeln('Ё                Ё            Ё             Ё               Ё');
      writeln('цддддддддддддддддеддддддддддддедддддддддддддеддддддддддддддд╢');
      writeln('Ё                Ё            Ё             Ё               Ё');
      writeln('цддддддддддддддддеддддддддддддедддддддддддддеддддддддддддддд╢');
      writeln('Ё                Ё            Ё             Ё               Ё');
      writeln('цддддддддддддддддеддддддддддддедддддддддддддеддддддддддддддд╢');
      writeln('Ё                Ё            Ё             Ё               Ё');
      writeln('юддддддддддддддддаддддддддддддадддддддддддддаддддддддддддддды');
      GOTOXY(4,3);
      TEXTCOLOR(RED);
      WRITE('JENIS MEMORI');
      GOTOXY(20,3);
      WRITE('TOTAL');
      GOTOXY(34,3);
      WRITE('DIGUNAKAN');
      GOTOXY(47,3);
      WRITE('FREE SPACE');
      TEXTCOLOR(YELLOW);
      GOTOXY(20,5);
      WRITELN('',CT,'');
      GOTOXY(34,5);
      WRITELN('',CD,'');
      GOTOXY(47,5);
      WRITELN('',CT-CD,'');
      GOTOXY(20,7);
      WRITELN('',UT,'');
      GOTOXY(34,7);
      WRITELN('',UD,'');
      GOTOXY(47,7);
      WRITELN('',UT-UD,'');
      GOTOXY(20,9);
      WRITELN('',RT,'');
      GOTOXY(34,9);
      WRITELN('',RD,'');
      GOTOXY(47,9);
      WRITELN('',RT-RD,'');
      GOTOXY(20,11);
      WRITELN('',ET,'');
      GOTOXY(34,11);
      WRITELN('',ED,'');
      GOTOXY(47,11);
      WRITELN('',ET-ED,'');
      GOTOXY(20,13);
      WRITELN('',TT,'');
      GOTOXY(34,13);
      WRITELN('',TD,'');
      GOTOXY(47,13);
      WRITELN('',TF,'');
      TEXTCOLOR(GREEN);
      GOTOXY(3,5);
      WRITE('CONVENSIONAL');
      GOTOXY(3,7);
      WRITE('UPPER');
      GOTOXY(3,9);
      WRITE('RESERVED');
      GOTOXY(3,11);
      WRITE('EXTENDED (XMS)');
      GOTOXY(3,13);
      WRITE('TOTAL MEMORI');


      readln;
end.